/* Compile-time constants injected by Vite's `define` (cf. electron.vite.config.mjs and
   vite.preview.config.mjs). Read from package.json at build time so the renderer never
   imports it directly. */
declare const __APP_VERSION__: string
