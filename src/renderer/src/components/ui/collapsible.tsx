/* Pas de surcharge : simple projection du primitive. La frontière ui/ reste la seule surface
   d'import (AUDIT.md §7, phase 5) — les features n'importent plus jamais ui/primitives/*
   directement, même quand ce niveau n'a rien à densifier. */
export { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@/components/ui/primitives/collapsible"
