/** Small-caps section label: collapsed group title (RefGroup), file list header,
    "commit"/"author" label in the detail panel (AUDIT.md §7, phase 5 — the same
    class combination used to be copied in four places). Meant to be composed with `cn()`, not to
    replace an element: each caller keeps its own layout (flex, margins, padding). */
export const LABEL_CLS = "text-[0.625rem] font-semibold tracking-[0.07em] text-muted-foreground uppercase"
