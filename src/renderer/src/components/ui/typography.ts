/** Label de section en petites capitales : titre de groupe replié (RefGroup), en-tête de liste
    de fichiers, libellé « commit »/« auteur » du panneau détail (AUDIT.md §7, phase 5 — la même
    combinaison de classes était recopiée à quatre endroits). À composer avec `cn()`, pas à
    remplacer un élément : chaque appelant garde son propre agencement (flex, marges, padding). */
export const LABEL_CLS = "text-[0.625rem] font-semibold tracking-[0.07em] text-muted-foreground uppercase"
