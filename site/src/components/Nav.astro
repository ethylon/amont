---
import { Image } from "astro:assets"
import Icon from "./Icon.astro"
import { ContrastIcon } from "@hugeicons/core-free-icons"
import { dict, REPO_URL, localePath, downloadPath, type Locale } from "../i18n/ui"
import icon from "../../../resources/icon.png"

interface Props {
  locale: Locale
  /** Per-locale route of the current page, so the language switch stays on it. */
  paths?: Record<Locale, string>
}

const { locale, paths = { en: "/", fr: "/fr/" } } = Astro.props
const t = dict[locale]

// Anchors carry the locale root so they also work from the download page.
const home = localePath(locale)
// `mobile` garde le lien visible sous 640 px : Download reste atteignable partout.
const links = [
  { href: `${home}#graph`, label: t.nav.graph, mobile: false },
  { href: `${home}#tour`, label: t.nav.tour, mobile: false },
  { href: downloadPath(locale), label: t.nav.download, mobile: true },
]
---

<header
  class="sticky top-0 z-50 border-b border-zinc-200/80 bg-white/80 backdrop-blur-md dark:border-zinc-800/80 dark:bg-zinc-950/80"
>
  <nav class="mx-auto flex h-16 max-w-6xl items-center gap-6 px-4 sm:px-6" aria-label="Main">
    <a href={localePath(locale)} class="flex items-center gap-2.5 font-semibold tracking-tight">
      <Image src={icon} alt="" width={56} densities={[1, 2]} class="size-7" loading="eager" />
      <span class="text-lg">Amont</span>
    </a>

    <div class="flex items-center gap-6 text-sm text-zinc-600 dark:text-zinc-400">
      {
        links.map((link) => (
          <a
            href={link.href}
            class:list={[
              !link.mobile && "hidden sm:block",
              "transition-colors hover:text-zinc-950 dark:hover:text-white",
            ]}
          >
            {link.label}
          </a>
        ))
      }
    </div>

    <div class="ml-auto flex items-center gap-1 text-sm">
      <a
        href={REPO_URL}
        rel="noopener"
        class="hidden rounded-md px-2.5 py-1.5 text-zinc-600 transition-colors hover:bg-zinc-100 hover:text-zinc-950 sm:block dark:text-zinc-400 dark:hover:bg-zinc-800/60 dark:hover:text-white"
      >
        GitHub <span aria-hidden="true">↗</span>
      </a>

      <div
        class="flex items-center gap-0.5 rounded-md border border-zinc-200 p-0.5 font-mono text-xs dark:border-zinc-800"
        role="group"
        aria-label="Language"
      >
        <a
          href={paths.en}
          aria-current={locale === "en" ? "page" : undefined}
          class:list={[
            "rounded px-1.5 py-1 transition-colors",
            locale === "en"
              ? "bg-zinc-100 font-semibold text-zinc-950 dark:bg-zinc-800 dark:text-white"
              : "text-zinc-500 hover:text-zinc-950 dark:hover:text-white",
          ]}
        >
          EN
        </a>
        <a
          href={paths.fr}
          aria-current={locale === "fr" ? "page" : undefined}
          class:list={[
            "rounded px-1.5 py-1 transition-colors",
            locale === "fr"
              ? "bg-zinc-100 font-semibold text-zinc-950 dark:bg-zinc-800 dark:text-white"
              : "text-zinc-500 hover:text-zinc-950 dark:hover:text-white",
          ]}
        >
          FR
        </a>
      </div>

      <button
        id="theme-toggle"
        type="button"
        aria-label={t.nav.theme}
        class="grid size-8 place-items-center rounded-md text-zinc-600 transition-colors hover:bg-zinc-100 hover:text-zinc-950 dark:text-zinc-400 dark:hover:bg-zinc-800/60 dark:hover:text-white"
      >
        <Icon icon={ContrastIcon} class="size-4.5" />
      </button>
    </div>
  </nav>
</header>

<script>
  document.getElementById("theme-toggle")?.addEventListener("click", () => {
    const dark = document.documentElement.classList.toggle("dark")
    localStorage.setItem("theme", dark ? "dark" : "light")
  })
</script>
