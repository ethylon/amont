pnpm-lock.yaml
out/
node_modules/
resources/
dist/

# Frozen historical documents (audit snapshot + refactor session prompts): their embedded
# code blocks quote past states of the codebase verbatim, reformatting would corrupt that record.
AUDIT.md
REFACTOR-PROMPTS.md

# Claude Code tooling: harness-managed launch config and skill docs (e.g. verify/SKILL.md,
# whose fenced code blocks hold literal DOM selectors — reformatting would corrupt them, same
# reason AUDIT.md is excluded above). Not project source; keep it out of the style gate.
.claude/
